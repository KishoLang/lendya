<div class="homeHeadContentNav fixed-top">
  <div class="navbar navbar-expand-lg navbar-light">
    <div class="navbar-brand">
      <img src="<%= image_path "lendya-logo1.png" %>" alt="Lendya Logo" class="brand-logo">
    </div>
    <div class="d-flex justify-content-end align-items-center m-4">
      <%= link_to 'Become a lender', new_item_path, :class => "nav-button-lender btn btn-warning" %>
      <% if user_signed_in? %>
      <!--<%= current_user.email %>-->
        <%= link_to 'SIGN OUT', destroy_user_session_path, method: 'delete', :class => "nav-text" %>
      <% else %>
        <%= link_to 'SIGN UP', new_user_registration_path, :class => "nav-text" %>
        <%= link_to 'LOGIN', new_user_session_path, :class => "nav-text"%>
      <% end %>
    </div>
  </div>
</div>

<div class="index-bg">
  <div class="text-center p-2">
    <h3>Check out all these friendly dogs &#128054;</h3>
    <h6>They are all super friendly, promise!</h6>
    <h6>Maybe there were one or three incidences, but we're super legit!</h6>
  </div>
  <div class="content d-flex justify-content-center">
    <% @items.each do |item| %>
    <div class="card d-inline-flex p-2 m-2 text-start" style="width: 18rem; border-radius: 15px; ">
      <img class="card-img-top" src="..." alt="Card image cap">
    <%#<% if item.photo.key.nil? %>
    <%#<% else %>
     <%# <%= cl_image_tag item.photo.key, class: "dog-avatar" %>
    <%#<% end %>
      <ul>
        <%= item.title %>
        <li><%= item.category %></li>
        <li>title: <%= item.title %></li>
        <li>price: <%= item.price %> € / hour</li>
      </ul>
      <p><%= link_to "Show more", item_path(item), :class => "btn btn-success"%></p>
    </div>
    <% end %>
  </div>

  <h3 class="text-center"><%= link_to "Go back", root_path, :class => "btn btn-link" %></h3>
</div>
