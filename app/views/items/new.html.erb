<div class="homeHeadContentNav">
  <div class="navbar navbar-expand-lg navbar-light">
    <div class="navbar-brand">
      <img src="<%= image_path "lendya-logo1.png" %>" alt="Lendya Logo" class="brand-logo">
    </div>
    <div class="d-flex justify-content-end align-items-center m-4">
      <%= link_to 'Become a lender', new_item_path, :class => "nav-button-lender btn btn-warning" %>
      <% if user_signed_in? %>
      <!--<%= current_user.email %>-->
        <%= link_to 'SIGN OUT', destroy_user_session_path, method: 'delete', :class => "nav-text" %>
      <% else %>
        <%= link_to 'SIGN UP', new_user_registration_path, :class => "nav-text" %>
        <%= link_to 'LOGIN', new_user_session_path, :class => "nav-text"%>
      <% end %>
    </div>
  </div>
</div>

<div class="container py-5">
  <div class="row">
    <div class="col-12 col-md-6 offset-lg-3">
      <div class="bg-grey rounded p-4">
          <h2>Let's make some casshhhhhh</h2>

            <%= simple_form_for(@item) do |f| %>
              <%= f.input :title %>
              <%= f.input :category %>
              <%= f.input :price %>
              <%= f.input :description %>
              <%= f.input :condition %>
              <%= f.input :delivery %>
              <%= f.input :long_rental %>
              <%= f.input :long_rental %>
              <%# <%= f.input :availability, as: :file %>

              <div class="btn btn-success p-2">
                <!--<%= f.submit %> !-->
                <%= f.button :submit, "Upload!" %>
              </div>

            <% end %>
            <%= link_to 'Back', items_path %>
      </div>
    </div>
  </div>
