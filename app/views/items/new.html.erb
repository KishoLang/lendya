  <div class="navbar navbar-expand-lg navbar-light">
    <div class="navbar-brand">
      <img src="<%= image_path "lendya-logo1.png" %>" alt="Lendya Logo" class="brand-logo">
    </div>
    <div class="d-flex justify-content-end align-items-center m-4">
      <%= link_to 'Become a lender', new_item_path, :class => "nav-button-lender btn btn-warning" %>
      <% if user_signed_in? %>
      <!--<%= current_user.email %>-->
        <%= link_to 'SIGN OUT', destroy_user_session_path, method: 'delete', :class => "nav-text" %>
      <% else %>
        <%= link_to 'SIGN UP', new_user_registration_path, :class => "nav-text" %>
        <%= link_to 'LOGIN', new_user_session_path, :class => "nav-text"%>
      <% end %>
    </div>
  </div>

</div>
<div class="container py-5">
  <div class="row">
    <div class="bordered col-12 col-md-6 offset-lg-3 justify-content-center bg-white p-5">
      <div class="shadow-md rounded px-8 pt-6 pb-8 mb-4">
          <div class="centered"> <h2>Share your item with the community!</h2></div>
            <%= simple_form_for(@item) do |f| %>
              <%= f.input :title, :class => "input-field" %>
              <%= f.input :category %>
              <%= f.input :price %>
              <%= f.input :description %>
              <%= f.input :condition %>
              <%= f.input :delivery %>
              <%= f.input :long_rental %>
              <%= f.input :availability %>
              <%# <%= f.input :photo, as: :file %>

      <div><p class="text-center text-gray-500 text-xs">
         Make sure that all information is correct!
          </p>
      </div>
          <div class="text-center upload-button">
            <%= f.button :submit, "Upload", class: "btn btn-warning nav-text" %>
          </div>
    </div>
            <div class="centered">
              <%= link_to 'Back', items_path %>
            </div>

      </div>
      </div>
  </div>


            <% end %>
